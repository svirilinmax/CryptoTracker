# Crypto Tracker

Система для отслеживания цен криптовалют с уведомлениями о выходе за заданные ценовые диапазоны.

## 🚀 Функциональность

- **Отслеживание криптовалют**: Добавление криптоактивов для мониторинга
- **Автоматическое обновление цен**: Фоновая задача обновляет цены с настраиваемым интервалом
- **История цен**: Сохранение и просмотр исторических данных по ценам с пагинацией
- **Пороговые уведомления**: Настройка минимальных и максимальных цен для получения уведомлений
- **REST API**: Полноценное API для интеграции с фронтендом и мобильными приложениями
- **JWT аутентификация**: Безопасная система аутентификации пользователей
- **Мониторинг ошибок**: Интеграция с Sentry для отслеживания ошибок и производительности

## 🛠 Технологический стек

### Backend
- **FastAPI** - современный, быстрый веб-фреймворк
- **SQLAlchemy 2.0** - ORM для работы с базой данных
- **Alembic** - система миграций базы данных
- **PostgreSQL** - основная база данных
- **Redis** - кэширование и брокер сообщений
- **JWT** - аутентификация
- **AIOHTTP** - асинхронные HTTP запросы к CoinGecko API
- **Worker** - фоновая обработка задач
- **Sentry** - мониторинг ошибок и производительности
- **Pydantic** - валидация данных с Field() ограничениями

### Frontend
- **HTML/CSS/JavaScript** - статические фронтенд файлы
- **Docker** - контейнеризация приложения

## 📁 Структура проекта

```
crypto_tracker/
├── backend/
│   ├── api_gateway/
│   │   ├── api/v1/endpoints/     # API endpoints (assets, auth)
│   │   ├── core/                 # Конфигурация, БД, безопасность
│   │   ├── crud/                 # Операции с БД (CRUD)
│   │   ├── models/               # Модели данных и схемы
│   │   └── services/             # Бизнес-логика (price_service)
│   ├── worker/                   # Фоновые задачи
│   └── docker-compose.yml
│── alembic/                      # Миграции базы данных
├── frontend/                     # Статические файлы фронтенда
├── tests/                        # Тесты
└── requirements.txt
```

## 🚀 Быстрый старт

### Предварительные требования

- Docker и Docker Compose
- API ключ от CoinGecko (бесплатный)
- Аккаунт Sentry (бесплатный) - для мониторинга ошибок

### Установка и запуск

1. **Клонирование и настройка**:
```bash
git clone https://github.com/svirilinmax/CryptoTracker.git
cd crypto_tracker
```

2. **Настройка переменных окружения**:
```bash
cd backend
cp .env.example .env
```
Отредактируйте `.env` файл:
```env
CRYPTO_API_KEY=your_coingecko_api_key_here
JWT_SECRET=your_super_secret_jwt_key
SECRET_KEY=your_fastapi_secret_key
SENTRY_DSN=your_sentry_dsn_here
PRICE_UPDATE_INTERVAL=300
WORKER_ERROR_DELAY=60
DATABASE_URL=postgresql://crypto_user:crypto_password@postgres:5432/crypto_db
REDIS_URL=redis://redis:6379/0
```

3. **Запуск приложения**:
```bash
docker-compose up --build -d
```

Приложение будет доступно:
- **API**: http://localhost:8005
- **Frontend**: http://localhost:8080
- **API Documentation**: http://localhost:8005/docs
- **Health Check**: http://localhost:8005/health
- **Sentry Test**: http://localhost:8005/sentry-debug

## 📚 API Endpoints

### Аутентификация
- `POST /api/v1/auth/register` - Регистрация пользователя
- `POST /api/v1/auth/login` - Вход в систему
- `GET /api/v1/auth/me` - Получение информации о текущем пользователе

### Управление активами
- `GET /api/v1/assets/` - Получить активные активы
- `GET /api/v1/assets/all` - Получить все активы (включая неактивные)
- `POST /api/v1/assets/` - Создать новый актив
- `GET /api/v1/assets/{asset_id}` - Получить конкретный актив
- `PUT /api/v1/assets/{asset_id}` - Обновить актив
- `DELETE /api/v1/assets/{asset_id}` - Удалить актив (деактивировать)
- `POST /api/v1/assets/{asset_id}/restore` - Восстановить актив
- `GET /api/v1/assets/{asset_id}/history` - Получить историю цен с пагинацией
- `POST /api/v1/assets/{asset_id}/refresh` - Принудительное обновление цены

### Системные
- `GET /health` - Проверка здоровья приложения
- `GET /sentry-debug` - Тестовый endpoint для проверки Sentry

## 💾 База данных и миграции

### Миграции с Alembic
Проект использует Alembic для управления миграциями базы данных:

```bash
# Применить миграции
docker-compose exec api alembic upgrade head

# Создать новую миграцию
docker-compose exec api alembic revision --autogenerate -m "Описание изменений"

# Откатить миграцию
docker-compose exec api alembic downgrade -1

# Проверить текущую миграцию
docker-compose exec api alembic current
```

### Автоматические миграции
При запуске контейнеров миграции применяются автоматически через entrypoint скрипты.

### Модели данных

#### Пользователь (User)
- id
- username (3-50 символов)
- email
- password_hash
- is_active
- created_at

#### Актив (Asset)
- id
- user_id
- symbol (2-10 заглавных букв)
- min_price (положительное число)
- max_price (положительное число, > min_price)
- current_price
- is_active
- created_at

#### История цен (PriceHistory)
- id
- asset_id
- price (положительное число)
- recorded_at

## ✅ Валидация данных

### Входные данные
Все API endpoints используют строгую валидацию через Pydantic:

- **Символы валют**: только заглавные буквы, 2-10 символов
- **Цены**: положительные числа, max_price > min_price
- **Пароли**: минимум 6 символов
- **Имена пользователей**: 3-50 символов
- **Пагинация**: автоматическая проверка границ (skip ≥ 0, 1 ≤ limit ≤ 1000)

### Обработка ошибок
- **IntegrityError**: обрабатываются при дублировании данных
- **ValidationError**: детальные сообщения об ошибках валидации
- **HTTPException**: стандартизированные HTTP коды ошибок

## 🔒 Безопасность

- Пароли хэшируются с использованием PBKDF2-SHA256
- JWT токены со сроком действия 7 дней
- Валидация входных данных через Pydantic с Field() валидацией
- **Конфиденциальные данные** (Sentry DSN, API ключи) хранятся в переменных окружения
- CORS настройки для фронтенда
- Обработка IntegrityError для предотвращения утечек данных

## 📊 Мониторинг с Sentry

### Что отслеживается:
- **Все необработанные исключения** в API
- **Производительность** HTTP запросов
- **Ошибки базы данных**
- **Проблемы с внешними API** (CoinGecko)
- **Ошибки аутентификации** и авторизации

### Конфигурация Sentry:
Sentry DSN настраивается через переменную окружения `SENTRY_DSN` в `.env` файле

### Тестирование Sentry:
```bash
# Вызвать тестовую ошибку для проверки Sentry
curl http://localhost:8005/sentry-debug
```

## 🔄 Фоновые задачи

Система включает воркер (`PriceUpdateWorker`), который:
- Автоматически обновляет цены всех активных активов
- **Интервал настраивается** через `PRICE_UPDATE_INTERVAL` в `.env` (по умолчанию 300 секунд)
- Логирует процесс обновления
- Обрабатывает ошибки API с настраиваемой задержкой при ошибках
- Отправляет ошибки в Sentry при возникновении проблем
- Использует асинхронные запросы для эффективной работы

## 🔧 Конфигурация

### Поддерживаемые криптовалюты
Система поддерживает основные криптовалюты через CoinGecko API:
- BTC (Bitcoin)
- ETH (Ethereum)
- ADA (Cardano)
- DOT (Polkadot)
- SOL (Solana)

### Настройка интервалов
- `PRICE_UPDATE_INTERVAL` - интервал обновления цен в секундах (по умолчанию 300)
- `WORKER_ERROR_DELAY` - задержка при ошибках воркера в секундах (по умолчанию 60)

## 🐛 Troubleshooting

### Распространенные проблемы

1. **Ошибки подключения к БД**:
   - Проверьте, что PostgreSQL контейнер запущен
   - Убедитесь в правильности DATABASE_URL в .env
   - Проверьте логи: `docker-compose logs postgres`

2. **Ошибки CoinGecko API**:
   - Проверьте правильность API ключа в .env
   - Убедитесь, что не превышен лимит запросов
   - Проверьте Sentry для деталей ошибок

3. **Проблемы с аутентификацией**:
   - Проверьте JWT_SECRET в .env файле
   - Убедитесь, что токены не истекли

4. **Проблемы с Sentry**:
   - Проверьте DSN в .env файле
   - Убедитесь, что интернет соединение активно
   - Проверьте `/sentry-debug` endpoint

5. **Ошибки валидации данных**:
   - Проверьте, что символы валют в верхнем регистре (BTC, ETH)
   - Убедитесь, что max_price > min_price
   - Проверьте ограничения длины для символов (2-10 символов)

6. **Проблемы с миграциями**:
   - Убедитесь, что Alembic может найти файл конфигурации
   - Проверьте подключение к БД перед применением миграций
   - Используйте `alembic current` для проверки текущей миграции

### Полезные команды для диагностики

```bash
# Проверить статус всех сервисов
docker-compose ps

# Просмотреть логи API
docker-compose logs api

# Просмотреть логи Worker
docker-compose logs worker

# Проверить подключение к БД
docker-compose exec postgres psql -U crypto_user -d crypto_db -c "\dt"

# Проверить текущую миграцию
docker-compose exec api alembic current

# Тестовый запрос к API
curl http://localhost:8005/health
```

## 🧪 Разработка

### Установка для разработки
```bash
python -m venv .venv
source .venv/bin/activate  # Linux/Mac
# или
.venv\Scripts\activate     # Windows

pip install -r requirements.txt
```

### Code Quality
Проект использует pre-commit хуки для поддержания качества кода:
```bash
pre-commit install
pre-commit run --all-files
```

### Запуск тестов
```bash
pytest
```

## 📄 Лицензия

Этот проект выпущен на условиях лицензии MIT. Подробности см. здесь `LICENSE`.

## 🤝 Вклад в проект

1. Форкните репозиторий
2. Создайте feature ветку
3. Внесите изменения
4. Добавьте тесты
5. Запустите pre-commit хуки
6. Создайте Pull Request

---

**Crypto Tracker** - надежная и масштабируемая система для отслеживания криптовалютных активов с современным стеком технологий и лучшими практиками безопасности.
```
